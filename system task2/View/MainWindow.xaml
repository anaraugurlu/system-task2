<Window x:Class="system_task2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:system_task2"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600" Background="Bisque" >
    <Grid>
        <StackPanel>
            <WrapPanel Margin="0,0,0,10">
                <Label Content="From"
                       FontSize="16"
                       Margin="0,0,10,0"></Label>
                <TextBox x:Name="fromTxt"
                         Width="300"
                         Height="30"
                         Margin="0,0,10,0"></TextBox>
                <Button x:Name="fileBtn"
                        Content="File..."
                        FontSize="18"
                        Height="30"
                        Width="60"
                        Background="BurlyWood" 
                        Command="{Binding FromBtn}"></Button>
            </WrapPanel>
            <WrapPanel>
                <Label Content="To"
                       FontSize="16"
                       Margin="0,0,29,0"></Label>
                <TextBox x:Name="toTxt"
                         Width="300"
                         Height="30"
                         Margin="0,0,10,0"></TextBox>
                <Button x:Name="file2Btn"
                        Content="File..."
                        FontSize="18"
                        Height="30"
                        Width="60"
                            Background="BurlyWood" 
                        Command="{Binding ToBtn}"></Button>
            </WrapPanel>
            <WrapPanel>
                <ProgressBar x:Name="progressBar"
                             Width="348"
                             Height="30"
                             Margin="10"></ProgressBar>
                <Button x:Name="CopyBtn"
                        Content="Copy"
                        FontSize="18"
                            Background="BurlyWood" 
                        Height="30"
                        Width="60"
                        Command="{Binding CopyBtn}"></Button>
            </WrapPanel>
            <WrapPanel>
                <Button x:Name="pauseBtn"
                        Content="Pause"
                        FontSize="18"
                        Height="30"
                        Width="100"
                        Margin="60,0,0,0"
                            Background="BurlyWood" 
                        Command="{Binding PauseBtn}"></Button>
                <Button x:Name="resumeBtn"
                        Content="Resume"
                        FontSize="18"
                            Background="BurlyWood" 
                        Height="30"
                        Width="100"
                        Margin="100,0,0,0"
                        Command="{Binding ResumeBtn}"></Button>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
